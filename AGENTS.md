# Codex 项目维护说明

本项目是“AI 医疗助手后台管理系统”前端原型，目标是模拟真实、可操作的医疗 AI 运营后台。

后续修改必须遵循以下约束：

1. 保持 React + Vite + TypeScript + Tailwind CSS 技术栈。
2. 保持当前深色医疗科技风格和现有页面结构，除非用户明确要求重构。
3. 医疗业务字段必须真实、具体，并与当前菜单业务一致。
4. 禁止为不同页面复用“名称、状态、生效日期、数据来源、附件、备注”一类通用弹窗字段。
5. 新增和编辑表单应通过 `src/mock/formSchemas.ts` 为每个菜单分别配置。
6. 列表字段、筛选条件、表单字段、详情 Tab 和保存后的记录映射必须保持一致。
7. 新增记录应使用符合业务语义的 ID，例如 `HEA-`、`MR-`、`RP-`、`AI-`、`DOC-` 和 `RULE-EMG-`。
8. 必填字段必须校验；下拉、单选和多选必须提供真实业务选项。
9. 新增、编辑和删除必须更新前端本地状态，不能仅显示提示消息。
10. 医疗报告内容不得进行无依据推断。例如 `OU` 只表示双眼，不能自行解释为阳性。
11. Mock 数据不得使用真实敏感患者信息，且必须明确用于原型演示。
12. 修改完成后至少运行 `npm run build`，确保 TypeScript 和 Vite 构建通过。
